name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Your push was successful!"
      - name: multiline script
        run: |
          node -v
          npm -v